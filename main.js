document.addEventListener('DOMContentLoaded', () => { const form = document.getElementById('signup-form'); const landingPage = document.getElementById('landing-page'); const successPage = document.getElementById('success-page'); form.addEventListener('submit', (e) => { e.preventDefault(); const button = form.querySelector('button'); const originalText = button.textContent; button.textContent = 'Submitting Application...'; button.disabled = true; const formData = new FormData(form); fetch(form.getAttribute('action'), { method: form.method, body: formData, headers: { 'Accept': 'application/json' } }).then(response => { if (response.ok) { showSuccess(); } else { alert('Oops! There was a problem submitting your form'); button.textContent = originalText; button.disabled = false; } }); function showSuccess() { landingPage.classList.remove('active'); landingPage.classList.add('hidden'); successPage.classList.remove('hidden'); setTimeout(() => { successPage.classList.add('active'); }, 50); } }); });